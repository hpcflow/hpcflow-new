
[tool.poetry]
name = "hpcflow"
version = "0.3.0a3"

description = "Computational workflow management"
authors = ["aplowman <adam.plowman@manchester.ac.uk>"]
license = "MPL-2.0"
readme = "README.md"
packages = [
    { include = "hpcflow" }
]
exclude = ["hpcflow/data/demo_data"]

[tool.poetry.dependencies]
python = ">=3.10,<3.14"
zarr = "2.17.2"
numpy = [
    {version = "^1.26.4", python = "<3.13"},
    {version = "^2.2.4", python = ">=3.13"},
]
scipy = [
    {version = "^1.13.1", python = "<3.10"},
    {version = "^1.15.2", python = ">=3.10"},
]
ruamel-yaml = "^0.18.17"
colorama = "^0.4.6"
termcolor = ">=1.1,<4.0"
fsspec = "^2026.4.0"
pytest = {version = ">=9.0.3,<10.0", optional = true}
click = "^8.3.3"
msgpack = "^1.0.4"
reretry = "^0.11.8"
psutil = ">=5.9.4,<8.0.0"
watchdog = "^6.0.0"
paramiko = ">=3.2,<6.0"
aiohttp = "^3.13.5"
rich = ">=15.0.0,<16.0.0"
h5py = "^3.16.0"
valida = "^0.7.5"
requests = "^2.33.1"
platformdirs = "^4.9.6"
pyzmq = ">=26.0.3,<28.0.0"
typing-extensions = "^4.12.2"
numcodecs = [
    {version = "0.12.1", python = "<3.13"},
    {version = "^0.13.1", python = ">=3.13"},
]
jinja2 = "^3.1.6"
ipywidgets = "^8.1.8"

[tool.poetry.group.dev.dependencies]
pylint = ">=2.12.2,<5.0.0"
pytest = ">=9.0.3,<10.0"
black = ">=25.1,<27.0"
commitizen = ">=4.15.0,<5.0.0"
pre-commit = ">=4.6.0,<5.0"
tuna = "^0.5.13"
sphinx = ">=4,<9"
sphinx-click = ">=4.4,<7.0"
sphinx-jinja = "^2.0.1"
sphinx-copybutton = "^0.5.0"
sphinx-design = ">=0.4.1,<0.7.0"
sphinx-togglebutton = "^0.4.5"
pydata-sphinx-theme = ">=0.17.1,<0.20.0"
ipykernel = "^7.2.0"
myst-nb = [
    {version = "0.17.2", python = "<3.9"},
    {version = "1.1.1", python = ">=3.9"}
]

[tool.poetry.group.test.dependencies]
pytest = ">=9.0.3,<10.0"
pexpect = "^4.8.0"
papermill = "^2.7.0"
ipykernel = "^7.2.0"

[tool.poetry.group.pyinstaller.dependencies]
pyinstaller = "^6.20.0"

[tool.poetry.group.typecheck]
optional = true
[tool.poetry.group.typecheck.dependencies]
types-colorama = "^0.4.15.20260408"
types-paramiko = ">=4.0.0.20260408,<6.0.0.0"
types-psutil = ">=7.2.2.20260408,<8.0.0.0"
types-pyinstaller = "^6.20.0.20260503"
types-requests = "^2.33.0.20260503"
types-setuptools = ">=82.0.0.20260408,<85.0.0.0"
mypy = ">=1.20.2,<3.0.0"
types-markupsafe = "^1.1.10"
scipy-stubs = {version = "^1.15.2.1", python = ">=3.10"}

[tool.poetry.extras]
test = ["pytest"]

[tool.poetry.scripts]
hpcflow = 'hpcflow.cli:cli'

[tool.poetry.plugins.pyinstaller40]
hook-dirs = "hpcflow.__pyinstaller:get_hook_dirs"

[tool.commitizen]
name = "cz_customize"
version = "0.3.0a3"
tag_format = "v$version"
version_files = [
    "pyproject.toml:version",
    "hpcflow/_version.py"
]
bump_message = "bump: $current_version → $new_version [skip ci]"

[tool.commitizen.customize]
bump_pattern = "^(BREAKING[ -]CHANGE|feat|fix|build|change|chore|ci|deprecate|docs|perf|refactor|remove|revert|security|style|test)(\\(.+\\))?(!)?:"

[tool.commitizen.customize.bump_map]
"BREAKING CHANGE" = "MAJOR"
"BREAKING-CHANGE" = "MAJOR"
feat = "MINOR"
fix = "PATCH"
build = "PATCH"
change = "PATCH"
chore = "PATCH"
ci = "PATCH"
deprecate = "PATCH"
docs = "PATCH"
perf = "PATCH"
refactor = "PATCH"
remove = "PATCH"
revert = "PATCH"
security = "PATCH"
style = "PATCH"
test = "PATCH"

[tool.black]
line-length = 90

[tool.mypy]
incremental = false

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
