rules:
  - path: []
    condition:
      {
        value.allowed_keys:
          [
            type,
            sub_parameters,
            is_file,
            dtype,
            shape,
            _hash_value,
            _validation,
            _is_hidden,
          ],
      }
